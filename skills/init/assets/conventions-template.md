<!-- Generated by shipit init on YYYY-MM-DD. Edit freely — refresh will show a
     diff and ask before overwriting anything you changed. -->

# Conventions

Patterns this repo actually follows. Each one names an exemplar; an implementer
reads the exemplar and copies its shape.

Rules:

- No convention without an exemplar at `path:line`.
- A pattern followed by roughly half the files is not a convention. Write it in
  **Not conventions** below instead.
- Nothing here restates an agent doc from `docs.agent_docs`.

## Structure

| Convention | Exemplar | How to apply |
| --- | --- | --- |
| | `path:line` | |

## Naming

| Convention | Exemplar | How to apply |
| --- | --- | --- |
| | `path:line` | |

## Tests

Test shape per kind lives in `rules/tests/<kind>.md`. Only cross-cutting test
conventions go here.

| Convention | Exemplar | How to apply |
| --- | --- | --- |
| | `path:line` | |

## Branch and commit

| Convention | Evidence | How to apply |
| --- | --- | --- |
| Commit subject | `git log` sample, N of 50 | |
| Branch name | `git for-each-ref` sample | |

## Not conventions

Patterns that looked like conventions and are not. Recorded so nobody enforces a
coin flip, and so `plan` knows to ask rather than assume.

| Pattern | Split | Verdict |
| --- | --- | --- |
| | e.g. 12 files vs 14 files | no convention — ask |
