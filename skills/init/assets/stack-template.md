<!-- Generated by shipit init on YYYY-MM-DD. Edit freely — refresh will show a
     diff and ask before overwriting anything you changed. -->

# Stack

One row per fact. A row without evidence does not belong here.

Only what the agent docs listed in `docs.agent_docs` do **not** already state.
Duplicating them creates a second source of truth that drifts.

| Fact | Value | Evidence |
| --- | --- | --- |
| Language | | `path:line` |
| Runtime version | | pin file, not what is installed locally |
| Package manager | | lockfile |
| Framework | | declared dependency, `path:line` |
| Database | | config or dependency, `path:line` |
| Test framework | | dependency + CI invocation |
| Lint | | CI job, `path:line` |
| CI | | workflow file |
| Deploy | | config, `path:line` |

## Verified commands

The command, and the proof it runs here. A command with no proof row was not
written to `config.json`.

| Purpose | Command | Proof | Exit |
| --- | --- | --- | --- |
| test (all) | | `version` \| `help` \| `dry-run` \| `real` | 0 |
| test (one) | `… {path}` | `real` | 0 |
| lint | | | |
| typecheck | | | |
| security | | | |
| coverage gate | | | |

## Undetermined

Every key that could not be determined, and what would settle it. This list is
the honest part of the file — an empty section here should be rare.

| Key | Why undetermined | What would settle it |
| --- | --- | --- |
| | | |

## Where the docs and the repo disagree

The repo wins. Recorded so someone can fix the doc.

| Doc claim | Reality | Evidence |
| --- | --- | --- |
| | | |
