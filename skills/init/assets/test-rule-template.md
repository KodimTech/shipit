<!-- Generated by shipit init on YYYY-MM-DD for test kind: <kind>
     Edit freely — refresh shows a diff and asks before overwriting. -->

# Test kind: <kind>

Framework: `<framework>`
Location: `<mirrored | colocated>` · Naming: `<pattern>`
Exemplar: `<path:line>`
Run one: `<commands.test_one with {path} substituted for the exemplar>`

## Shape

Reduced from the exemplar. This is a description of what the repo does, not a
recommendation of what it should do.

```
<The exemplar's skeleton: setup block, subject, grouping, assertion style.
Structure only — strip the domain specifics.>
```

## Rules

1. <rule> — `path:line`
2. <rule> — `path:line`

## Fixtures and doubles

How this kind gets its data, taken from real files:

| Need | How this repo does it | Exemplar |
| --- | --- | --- |
| Object under test | | `path:line` |
| Collaborator | | `path:line` |
| Database access | allowed \| forbidden \| unclear | `path:line` |
| External HTTP | | `path:line` |

`unclear` is a valid answer and better than a guess.

## Coverage

Gates the merge: `<yes | no>`
Command: `<commands.coverage_gate>`

<If it gates: what makes it fail — uncovered changed lines, a global threshold,
branch coverage. This is the most common cause of red CI on otherwise correct
work, so state it precisely.>

## Undetermined

<What one exemplar could not settle. Delete if nothing.>
