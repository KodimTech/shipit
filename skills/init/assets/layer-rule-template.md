<!-- Generated by shipit init on YYYY-MM-DD for layer: <key>
     Edit freely — refresh shows a diff and asks before overwriting. -->

# Layer: <key>

Directories: `<dir>`, `<dir>`
Exemplar: `<path:line>`
Test kind: `<kind>` → `rules/tests/<kind>.md` (or `none`)

## What lives here

<One or two sentences. What responsibility this directory holds, read off the
exemplar and its neighbours — not off the directory's name.>

## Shape

Only what the exemplar demonstrates. If the exemplar does not show it, it does not
go here.

- Entry point / public interface: <method, export, or handler the exemplar exposes>
- Dependencies: <how the exemplar receives collaborators>
- Return shape: <what the exemplar returns>
- Error handling: <what the exemplar does on failure>
- Persistence: <does the exemplar write, or is it read-only>

## Rules

Numbered, each traceable to a file. A rule nobody can point at is an opinion.

1. <rule> — `path:line`
2. <rule> — `path:line`

## Counter-examples

Files in this layer that break the shape above, and whether they are the
exception or the emerging new pattern. An implementer needs to know which one to
copy.

| File | How it differs | Verdict |
| --- | --- | --- |
| `path:line` | | exception \| new pattern \| unclear |

## Undetermined

<What the exemplar does not settle. Written as `unknown` so `plan` asks instead of
guessing. Delete this section if nothing is undetermined.>
